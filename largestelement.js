let arr = [100, 5, 36, 89, 0];
let largest = arr[0];
let secondlargest = arr[0];

for(let i = 0; i<arr.length;i++){
    if(arr[i]> largest)
    {
    largest = arr[i];
    }
  
}
    console.log("largest " +largest);
    
